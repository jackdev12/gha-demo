name: Hello World
on:
  push:
    branches:
      - main
      - develop
      - 'release/**'
  pull_request:
    branches:
      - main
name: Github Action Demo
on: push
jobs:
  action-demo:
    runs-on: ubuntu-latest
    steps:
      - name: checkout the code
        uses: actions/checkout@v4
      - name: printing index.html file
        run: cat index.html
      - name: Build Step
        run: echo "Hello, ${{vars.My_NAME}}"
